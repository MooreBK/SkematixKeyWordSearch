This process reads an html extract of the skematix flowcharts. It reads between flowchart beginning and ending tags to find keywords. 
If found it writes a csv file where you can review the jobs. Each line written is the line that had the keyword in it.
